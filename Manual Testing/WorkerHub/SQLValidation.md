# SQL Validation Queries

## Verify Registered Users

```sql
SELECT * FROM users;
```

## Verify Posted Jobs

```sql
SELECT * FROM jobs;
```

## Verify Job Applications

```sql
SELECT * FROM applications;
```

## Verify Notifications

```sql
SELECT * FROM notifications;
```

## Verify User Profile

```sql
SELECT * FROM users WHERE username='worker1';
```
